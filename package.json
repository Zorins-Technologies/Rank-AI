{
  "name": "rank-ai-root",
  "version": "1.0.0",
  "description": "Root orchestration for Rank AI Full-Stack Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"yellow,cyan\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "clean": "npx kill-port 8000 3000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "pg": "^8.20.0"
  }
}
